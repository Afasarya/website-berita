import { createClient } from "@/lib/supabase/server";
export default async function AdminUsers() { const supabase = await createClient(); const { data: users } = await supabase.from("profiles").select("id,full_name,role,created_at").order("created_at", { ascending: false }); return <main className="mx-auto w-full max-w-5xl px-4 py-12 sm:px-8"><h1 className="text-3xl font-bold">User terdaftar</h1><div className="mt-8 overflow-x-auto rounded-xl border border-border bg-white"><table className="w-full min-w-[600px] text-left text-sm"><thead className="border-b border-border bg-[#f8f9fb]"><tr><th className="p-4">Nama</th><th className="p-4">Role</th><th className="p-4">Tanggal daftar</th></tr></thead><tbody>{users?.map((user) => <tr key={user.id} className="border-b border-border"><td className="p-4">{user.full_name}</td><td className="p-4">{user.role}</td><td className="p-4">{new Date(user.created_at).toLocaleDateString("id-ID")}</td></tr>)}</tbody></table></div></main>; }
