import { createClient } from "@/lib/supabase/server";
import CategoryActions from "./category-actions";
export default async function CategoriesPage() { const supabase = await createClient(); const { data: categories } = await supabase.from("categories").select("id,name,slug,created_at").order("name"); return <main className="mx-auto w-full max-w-4xl px-4 py-12 sm:px-8"><h1 className="text-3xl font-bold">Kelola kategori</h1><CategoryActions /><div className="mt-8 overflow-x-auto rounded-xl border border-border bg-white"><table className="w-full text-left text-sm"><thead className="border-b border-border bg-[#f8f9fb]"><tr><th className="p-4">Nama</th><th className="p-4">Slug</th><th className="p-4">Aksi</th></tr></thead><tbody>{categories?.map((category) => <tr key={category.id} className="border-b border-border"><td className="p-4">{category.name}</td><td className="p-4">{category.slug}</td><td className="p-4"><CategoryActions id={category.id} /></td></tr>)}</tbody></table></div></main>; }
