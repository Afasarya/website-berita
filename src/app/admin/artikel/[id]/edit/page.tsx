import { notFound, redirect } from "next/navigation";
import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import AdminArticleForm from "../../article-form";
export default async function EditAdminArticle({ params }: { params: Promise<{ id: string }> }) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: article } = await supabase.from("articles").select("id,title,content,category_id,source_note,is_featured,thumbnail_url").eq("id", (await params).id).single(); if (!article) notFound(); const { data: categories } = await supabase.from("categories").select("id,name").order("name"); return <main className="mx-auto w-full max-w-3xl px-4 py-12 sm:px-8"><Link href="/admin/artikel" className="text-sm text-[#1e3a5f]">← Kembali</Link><h1 className="mt-4 text-3xl font-bold">Edit artikel</h1><AdminArticleForm categories={categories ?? []} authorId={user.id} article={article} /></main>; }
