"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
type Props = { article: { id: string; title: string; content: string; source_note: string | null; category_id: string; thumbnail_url: string }; categories: { id: string; name: string }[] };
export default function EditArticleForm({ article, categories }: Props) { const router = useRouter(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const title = String(form.get("title")).trim(); const content = String(form.get("content")).trim(); if (title.length < 15 || title.length > 120) return setError("Judul harus 15–120 karakter."); if (content.length < 200) return setError("Konten minimal 200 karakter."); setLoading(true); const { error } = await createClient().from("articles").update({ title, content, category_id: form.get("category"), source_note: form.get("source_note"), status: "pending", rejection_reason: null }).eq("id", article.id); if (error) setError(error.message); else router.push("/dashboard"); setLoading(false); } return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-medium">Judul<input name="title" defaultValue={article.title} required className="mt-2 w-full rounded-lg border border-border p-3" /></label><label className="block text-sm font-medium">Kategori<select name="category" defaultValue={article.category_id} className="mt-2 w-full rounded-lg border border-border p-3">{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="block text-sm font-medium">Konten<textarea name="content" defaultValue={article.content} required rows={14} className="mt-2 w-full rounded-lg border border-border p-3" /></label><label className="block text-sm font-medium">Sumber/referensi<textarea name="source_note" defaultValue={article.source_note ?? ""} rows={3} className="mt-2 w-full rounded-lg border border-border p-3" /></label>{error && <p className="text-sm text-[#b91c1c]">{error}</p>}<button disabled={loading} className="rounded-lg bg-[#1e3a5f] px-5 py-3 font-medium text-white">{loading ? "Menyimpan..." : "Kirim ulang untuk review"}</button></form>; }
